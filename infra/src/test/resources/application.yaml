app:
  security:
    enabled: false
  cors:
    allowed-origins: "http://localhost:3000,http://localhost:5173,http://localhost:4200"
  sync:
    ingest:
      base-url: http://localhost:9999  # stub — no real calls in unit tests
      api-key: test-key
      cron:
        enabled: false
      active-owner-window-days: 30
      page-size: 100
  pluggy:
    base-url: http://localhost:9999  # stub — no real calls in unit tests
    client-id: test-client-id
    client-secret: test-client-secret
