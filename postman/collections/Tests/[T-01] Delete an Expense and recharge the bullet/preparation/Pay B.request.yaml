$kind: http-request
name: Pay B
url: "{{baseUrl}}/pay"
method: POST
headers:
  Content-Type: application/json
queryParams:
  walletId: "{{walletId}}"
body:
  type: json
  content: |-
    {
      "payment": {
        "amount": 100.0,
        "paymentDate": "2026-04-13T12:00:00Z",
        "details": "T-01 B {{$randomUUID}}"
      },
      "bulletId": "{{bulletIdB}}",
      "expenseId": "{{expenseId}}"
    }
scripts:
  - type: afterResponse
    code: |-
      pm.test("Status code is 204", function () {
        pm.response.to.have.status(204);
      });
    language: text/javascript
order: 4492313521609398
