$kind: http-request
name: Rule 3 - Charges After Delete Empty
url: "{{baseUrl}}/wallets/{{walletAfterDeleteId}}/subscription-charges"
method: GET
scripts:
  - type: afterResponse
    code: |-
      pm.test("Status code is 200", function () {
        pm.response.to.have.status(200);
      });

      pm.test("No subscription charge is materialized after delete", function () {
        pm.expect(pm.response.json()).to.have.length(0);
      });
    language: text/javascript
order: 1776118299000
