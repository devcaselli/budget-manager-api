$kind: http-request
name: Rule 1 - Charges Before Start Month Empty
url: "{{baseUrl}}/wallets/{{walletBeforeSubscriptionId}}/subscription-charges"
method: GET
scripts:
  - type: afterResponse
    code: |-
      pm.test("Status code is 200", function () {
        pm.response.to.have.status(200);
      });

      pm.test("No charges before subscription start month", function () {
        pm.expect(pm.response.json()).to.have.length(0);
      });
    language: text/javascript
order: 1776118292000
