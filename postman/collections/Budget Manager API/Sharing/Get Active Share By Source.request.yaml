$kind: http-request
name: Get Active Share By Source
method: GET
url: "{{baseUrl}}/shares/active?sourceType=EXPENSE&sourceId={{expenseId}}"
order: 1775858143000
scripts:
  - type: afterResponse
    code: |-
      pm.test("Status code is 200 or 404", function () {
        pm.expect([200, 404]).to.include(pm.response.code);
      });
    language: text/javascript
