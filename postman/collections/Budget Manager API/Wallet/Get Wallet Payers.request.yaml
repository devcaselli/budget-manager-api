$kind: http-request
name: Get Wallet Payers
method: GET
url: "{{baseUrl}}/wallets/{{walletId}}/payers"
order: 1775858130000
scripts:
  - type: afterResponse
    code: |-
      pm.test("Status code is 200", function () {
        pm.response.to.have.status(200);
      });
    language: text/javascript
