$kind: http-request
name: Delete Installment
method: DELETE
url: "{{baseUrl}}/installments/{{installmentId}}"
order: 1775858129000
scripts:
  - type: afterResponse
    code: |-
      pm.test("Status code is 204", function () {
        pm.response.to.have.status(204);
      });
    language: text/javascript
