apiVersion: v1
kind: ConfigMap
metadata:
  name: manager-api-config
  namespace: {{ .Values.namespace }}
data:
  {{- range $key, $val := .Values.managerApi.env }}
  {{ $key }}: {{ $val | quote }}
  {{- end }}
